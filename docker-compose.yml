version: '3.4'

services:
  collactiontestselection:
    image: collactiontestselection
    env_file:
      - collactiontestselection.env
    ports:
      - '8080:8080'
    build:
      context: .
      dockerfile: CollactionTestSelection/Dockerfile